<Window x:Class="InvoiceApp.Views.TaxRateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:InvoiceApp.ViewModels"
        mc:Ignorable="d"
        Title="Tax Rates" MinHeight="300" MinWidth="500"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DataGrid x:Name="DataGrid"
                  ItemsSource="{Binding Rates}"
                  SelectedItem="{Binding SelectedRate}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="*" />
                <DataGridTextColumn Header="%" Binding="{Binding Percentage, UpdateSourceTrigger=PropertyChanged}" Width="Auto" />
                <DataGridTextColumn Header="From" Binding="{Binding EffectiveFrom, UpdateSourceTrigger=PropertyChanged}" Width="Auto" />
                <DataGridTextColumn Header="To" Binding="{Binding EffectiveTo, UpdateSourceTrigger=PropertyChanged}" Width="Auto" />
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="Add" Command="{Binding AddCommand}" Margin="4" />
            <Button Content="Delete" Command="{Binding DeleteCommand}" Margin="4" />
            <Button Content="Save" Command="{Binding SaveCommand}" Margin="4" />
        </StackPanel>
    </Grid>
</Window>
