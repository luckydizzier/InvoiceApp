<Window x:Class="InvoiceApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InvoiceApp" xmlns:viewModels="clr-namespace:InvoiceApp.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="Auto" Width="Auto"
        KeyDown="OnKeyDown">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="_Windows">
                <MenuItem Header="_Payment Methods" Click="OpenPaymentMethods" />
                <MenuItem Header="_Suppliers" Click="OpenSuppliers" />
                <MenuItem Header="_Product Groups" Click="OpenProductGroups" />
                <MenuItem Header="_Tax Rates" Click="OpenTaxRates" />
            </MenuItem>
        </Menu>

        <!-- Invoice list -->
        <ListView x:Name="InvoicesList"
                  ItemsSource="{Binding Invoices}"
                  SelectedItem="{Binding SelectedInvoice}"
                  Grid.Row="1" />

        <!-- Items of selected invoice -->
        <DockPanel Grid.Column="1" Grid.Row="1">
            <DataGrid x:Name="ItemsGrid"
                      ItemsSource="{Binding Items}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      RowDetailsVisibilityMode="VisibleWhenSelected"
                      Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Leírás" Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Width="*" />
                    <DataGridTextColumn Header="Mennyiség" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="Auto" />
                    <DataGridTextColumn Header="Egységár" Binding="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged}" Width="Auto" />
                    <DataGridTemplateColumn Header="Termék" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Item.Product.Name}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding DataContext.Products, RelativeSource={RelativeSource AncestorType=Window}}"
                                          SelectedItem="{Binding Item.Product, UpdateSourceTrigger=PropertyChanged}"
                                          DisplayMemberPath="Name" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
                <DataGrid.RowDetailsTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" Margin="5">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Név" Width="50" />
                                <TextBox Text="{Binding Item.Product.Name, UpdateSourceTrigger=PropertyChanged}" Width="200" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Nettó" Width="50" />
                                <TextBox Text="{Binding Item.Product.Net, UpdateSourceTrigger=PropertyChanged}" Width="100" />
                                <TextBlock Text="Bruttó" Width="50" Margin="10,0,0,0" />
                                <TextBox Text="{Binding Item.Product.Gross, UpdateSourceTrigger=PropertyChanged}" Width="100" />
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </DataGrid.RowDetailsTemplate>
            </DataGrid>
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
                <Button x:Name="AddItemButton" Content="Add" Click="AddItemClicked" Margin="4" />
                <Button x:Name="DeleteItemButton" Content="Delete" Click="DeleteItemClicked" Margin="4" />
            </StackPanel>
        </DockPanel>

        <StatusBar Grid.Row="2" Grid.ColumnSpan="2">
            <TextBlock Text="{Binding StatusMessage}" />
        </StatusBar>
    </Grid>
</Window>
