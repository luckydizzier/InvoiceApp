<Window x:Class="InvoiceApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InvoiceApp"
        xmlns:viewModels="clr-namespace:InvoiceApp.ViewModels"
        xmlns:views="clr-namespace:InvoiceApp.Views"
        xmlns:helpers="clr-namespace:InvoiceApp.Helpers"
        mc:Ignorable="d"
        Title="Számlakezelő"
        MinHeight="600" MinWidth="1000"
        helpers:FocusBehavior.IsFocused="{Binding IsInvoiceListFocused}">
    <Window.InputBindings>
        <KeyBinding Key="Up" Command="{Binding NavigateUpCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
        <KeyBinding Key="Down" Command="{Binding NavigateDownCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
        <KeyBinding Key="Enter" Command="{Binding EnterCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
        <KeyBinding Key="Escape" Command="{Binding EscapeCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
        <KeyBinding Key="Delete" Command="{Binding DeleteInvoiceCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
    </Window.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- == Menü == -->
            <RowDefinition Height="*" />
            <!-- == Tartalom == -->
            <RowDefinition Height="Auto" />
            <!-- == Státuszsor == -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <!-- == Számlalista == -->
            <ColumnDefinition Width="*" />
            <!-- == Számlatartalom == -->
        </Grid.ColumnDefinitions>

        <!-- Menü -->
        <Menu Grid.Row="0" Grid.ColumnSpan="2">
            <MenuItem Header="_Ablakok">
                <MenuItem Header="_Fizetési módok" Click="OpenPaymentMethods" />
                <MenuItem Header="_Szállítók" Click="OpenSuppliers" />
                <MenuItem Header="_Termékcsoportok" Click="OpenProductGroups" />
                <MenuItem Header="_Áfakulcsok" Click="OpenTaxRates" />
            </MenuItem>
        </Menu>

        <!-- Számlák listája -->
        <StackPanel Grid.Row="1" Grid.Column="0" Margin="4">
            <Button Content="🆕 Új számla" Command="{Binding NewInvoiceCommand}" Style="{StaticResource IconButtonStyle}" Margin="0,0,0,4"/>
            <ListView x:Name="InvoicesList"
                      ItemsSource="{Binding Invoices}"
                      SelectedItem="{Binding SelectedInvoice}"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Number}" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding Date, StringFormat=\"{0:d}\"}" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding Supplier.Name}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <!-- Kiválasztott számla tételei -->
        <Grid Grid.Row="1" Grid.Column="1" Margin="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Fejléc -->
                <RowDefinition Height="*"/>
                <!-- DataGrid -->
                <RowDefinition Height="Auto"/>
                <!-- Gombok -->
                <RowDefinition Height="Auto"/>
                <!-- Összegzés -->
            </Grid.RowDefinitions>

            <DataGrid x:Name="ItemsGrid"
                      Grid.Row="1"
                      ItemsSource="{Binding Items}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="True"
                      NewItemPlaceholder="{Binding NewItemCommand}"
                      RowDetailsVisibilityMode="VisibleWhenSelected"
                      Margin="0,0,0,4">
                <DataGrid.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding ItemsEnterCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                    <KeyBinding Key="Delete" Command="{Binding ItemsDeleteCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                </DataGrid.InputBindings>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Leírás" Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Width="2*"/>
                    <DataGridTextColumn Header="Mennyiség" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="Auto"/>
                    <DataGridTextColumn Header="Egységár" Binding="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged}" Width="Auto"/>
                    <DataGridTextColumn Header="Betétdíj" Binding="{Binding Deposit, UpdateSourceTrigger=PropertyChanged}" Width="Auto"/>
                    <DataGridTextColumn Header="Visszáru" Binding="{Binding Return, UpdateSourceTrigger=PropertyChanged}" Width="Auto"/>
                    <DataGridTemplateColumn Header="Termék" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Item.Product.Name}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding DataContext.Products, RelativeSource={RelativeSource AncestorType=Window}}"
                                          SelectedItem="{Binding Item.Product, UpdateSourceTrigger=PropertyChanged}"
                                          DisplayMemberPath="Name"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="ÁFA" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Item.TaxRate.Name}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding DataContext.TaxRates, RelativeSource={RelativeSource AncestorType=Window}}"
                                          SelectedItem="{Binding TaxRate, UpdateSourceTrigger=PropertyChanged}"
                                          DisplayMemberPath="Name"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="💾"
                                        Command="{Binding DataContext.SaveItemCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource IconButtonStyle}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>

                <DataGrid.RowDetailsTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" Margin="5">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Név" Width="50" />
                                <TextBox Text="{Binding Item.Product.Name, UpdateSourceTrigger=PropertyChanged}" Width="200" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Nettó" Width="50" />
                                <TextBox Text="{Binding Item.Product.Net, UpdateSourceTrigger=PropertyChanged}" Width="100" />
                                <TextBlock Text="Bruttó" Width="50" Margin="10,0,0,0" />
                                <TextBox Text="{Binding Item.Product.Gross, UpdateSourceTrigger=PropertyChanged}" Width="100" />
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </DataGrid.RowDetailsTemplate>
            </DataGrid>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
                <views:InvoiceHeaderView />
                <Button Content="➕ Szállító" Command="{Binding AddSupplierCommand}" Style="{StaticResource IconButtonStyle}" Margin="8,0,0,0" />
            </StackPanel>

            <!-- Gombok -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="➕ Tétel hozzáadása" Command="{Binding AddItemCommand}" Style="{StaticResource IconButtonStyle}"/>
                <Button Content="🗑️ Tétel törlése" Command="{Binding RemoveItemCommand}" Style="{StaticResource IconButtonStyle}"/>
                <Button Content="💾 Mentés" Command="{Binding SaveCommand}" Style="{StaticResource IconButtonStyle}"/>
            </StackPanel>

            <!-- Összegző panel -->
            <StackPanel Grid.Row="3" Orientation="Vertical" Margin="0,4,0,0">
                <Separator/>
                <Grid Margin="0,4,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="VAT Breakdown by Rate" Margin="0,0,0,4"/>
                        <DataGrid ItemsSource="{Binding VatBreakdown}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Margin="0,0,8,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="VAT %" Binding="{Binding Rate}" Width="Auto"/>
                                <DataGridTextColumn Header="Net Amount" Binding="{Binding Net}" Width="*" ElementStyle="{StaticResource RightAlignedCellStyle}"/>
                                <DataGridTextColumn Header="VAT Amount" Binding="{Binding Vat}" Width="*" ElementStyle="{StaticResource RightAlignedCellStyle}"/>
                                <DataGridTextColumn Header="Gross Amount" Binding="{Binding Gross}" Width="*" ElementStyle="{StaticResource RightAlignedCellStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                    <Grid Grid.Column="1" Margin="8,0,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Total Net:"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding TotalNet}" HorizontalAlignment="Right"/>
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Total VAT:"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding TotalVat}" HorizontalAlignment="Right"/>
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Total Gross:"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding TotalGross}" HorizontalAlignment="Right"/>
                    </Grid>
                </Grid>
                <TextBlock Text="{Binding InWords}" HorizontalAlignment="Right" Margin="0,4,0,0" TextWrapping="Wrap" FontStyle="Italic"/>
            </StackPanel>
        </Grid>

        <!-- Státuszsor -->
        <StatusBar Grid.Row="2" Grid.ColumnSpan="2">
            <TextBlock Text="{Binding StatusMessage}" />
        </StatusBar>
    </Grid>
</Window>
