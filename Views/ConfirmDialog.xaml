<Window x:Class="InvoiceApp.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:InvoiceApp.Helpers"
        Title="Confirm"
        MinHeight="150" MinWidth="350"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow" ResizeMode="NoResize"
        KeyDown="Window_KeyDown"
        FocusManager.FocusedElement="{Binding ElementName=YesButton}">
    <StackPanel Margin="10">
        <TextBlock x:Name="MessageText" TextWrapping="Wrap" Margin="0,0,0,10" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="YesButton" Content="Yes" Width="75" Margin="5,0" Click="Yes_Click"
                    helpers:FocusBehavior.IsFocused="True" IsDefault="True"/>
            <Button x:Name="NoButton" Content="No" Width="75" Margin="5,0"
                    IsCancel="True" Click="No_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
